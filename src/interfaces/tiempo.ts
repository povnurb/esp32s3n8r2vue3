export interface ITimeConfig {
    
        serial?: string; 
        device?: string,
        rssiStatus?: number,
        wifiQuality?: number,
        wifiStatus?: boolean,
        mqttStatus?: boolean,
        fecha?: string,
        code?: number


}